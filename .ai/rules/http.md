---
paths:
  - 'app/Http/**'
---

# Http

## Deterministic documentation fixtures
Keep documentation fixture endpoints read-only, local and deterministic. Validate every query parameter with a Form Request; never add persistence, authentication, external providers or package behavior in the host.
